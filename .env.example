MCP_HOST=0.0.0.0
MCP_PORT=4010
GAS_UP_API_BASE_URL=http://127.0.0.1:4000

# Optional fallback API key for internal deployments only.
# For user-facing deployments, pass each user's key per request with:
# X-API-Key: gs_live_...
# or Authorization: Bearer gs_live_...
GAS_UP_API_KEY=
